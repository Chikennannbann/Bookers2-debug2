<div class='container px-5 px-sm-0'>
  <div class='row'>
   <h2>Followings</h2>
    <% if @users.exists? %>
     <%= render 'users/index', users: @users %>
    <% else %>
    <p>ユーザーはいません</p>
    <% end %>
  </div>
</div>